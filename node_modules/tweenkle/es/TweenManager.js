import Sister from 'sister';

var TweenManager = function TweenManager(tweens) {
  if (tweens === void 0) {
    tweens = [];
  }

  var emitter = new Sister();
  this.on = emitter.on;
  this.off = emitter.off;
  this.trigger = emitter.trigger;
  this.tweens = tweens;
};

export default TweenManager;